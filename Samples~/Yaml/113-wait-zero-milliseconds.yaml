name: "113-wait-zero-milliseconds"
description: Covers wait action with 0ms duration boundary.
fixture:
  host_window:
    type: ExampleBasicLoginWindow
    reopen_if_open: true
steps:
  - action: wait
    duration: "0ms"
  - action: assert_text
    selector: "#status-label"
    expected: "Idle"
