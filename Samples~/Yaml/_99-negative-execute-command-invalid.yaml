name: "_99-negative-execute-command-invalid"
description: Expects failure when execute_command is missing the command parameter.
fixture:
  host_window:
    type: ExampleCoverageInputWindow
    reopen_if_open: true
steps:
  - action: execute_command
    selector: "#command-input"
