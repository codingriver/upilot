name: "_95-negative-menu-item-disabled-fail"
description: Expects failure when asserting menu item is disabled but it is enabled.
fixture:
  host_window:
    type: ExampleCoverageInputWindow
    reopen_if_open: true
steps:
  - action: menu_item
    selector: "#toolbar-menu"
    item: "Refresh"
    kind: popup
    mode: assert_disabled
