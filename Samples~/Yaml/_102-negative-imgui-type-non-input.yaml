name: "_102-negative-imgui-type-non-input"
description: Expects failure when imgui_assert_text expected does not match actual button text.
fixture:
  host_window:
    type: ImguiExampleWindow
    reopen_if_open: true
steps:
  - action: imgui_assert_text
    selector: 'gui(button, control_name="generate-button")'
    text: "wrong"
