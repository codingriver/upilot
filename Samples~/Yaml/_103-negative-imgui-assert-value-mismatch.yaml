name: "_103-negative-imgui-assert-value-mismatch"
description: Expects failure when imgui_assert_value expected does not match actual toggle value.
fixture:
  host_window:
    type: ImguiExampleWindow
    reopen_if_open: true
steps:
  - action: imgui_assert_value
    selector: 'gui(toggle, control_name="feature-toggle")'
    expected: "wrong"
