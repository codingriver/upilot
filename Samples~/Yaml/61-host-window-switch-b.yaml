name: "61-host-window-switch-b"
fixture:
  host_window:
    type: UnityUIFlow.SampleInteractionWindow
    reopen_if_open: true
steps:
  - action: assert_visible
    selector: '#interaction-root'
