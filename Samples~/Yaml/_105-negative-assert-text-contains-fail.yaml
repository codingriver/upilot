name: "_105-negative-assert-text-contains-fail"
description: Expects failure when assert_text_contains expected substring is not present.
fixture:
  host_window:
    type: ExampleBasicLoginWindow
    reopen_if_open: true
steps:
  - action: assert_text_contains
    selector: "#status-label"
    expected: "this-does-not-exist"
