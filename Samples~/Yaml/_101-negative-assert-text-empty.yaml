name: "_101-negative-assert-text-empty"
description: Expects failure when assert_text expected is empty but actual is not.
fixture:
  host_window:
    type: ExampleBasicLoginWindow
    reopen_if_open: true
steps:
  - action: assert_text
    selector: "#login-button"
    expected: ""
