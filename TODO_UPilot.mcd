# UPilot 活动改进清单

核对日期：2026-09-23。依据当前工作区源码、仓库 TODO、关联项目现场记录和已有定向验收记录重新整理。本文件是当前产品状态的唯一权威入口；旧标题中的 Open/已完成不直接决定当前状态。

## 口径与来源

- 初次整理仅分析文档；2026-09-23 后续批次已实施 D01/D03/D04/D05/D06/D07/D09 并编写定向用例，但未运行测试或行为验收，未刷新部署、发布或处置队列。源码存在不等于运行验证通过，编译成功不证明 Server 已加载相同源码。
- 已完成事项已从活动清单删除，原始正文及已有工作区增补完整保留在 [根清单历史](UPilot-Backlog-History-20260923.mcd)。历史中的“最新”“当前”和优先级仅代表记录当时，不覆盖本文件。
- 仓库内其余 TODO 的未完成事项已合并到本文件；[整合快照入口](Documentation~/TODO_UPilot_Integrated.md)仅保留整合引用，不再维护独立活动清单。项目级 `TODO_Distill.md` 与 `Distill-History-20260923.md` 的有效内容已完整收敛到 D09，并于 2026-09-24 删除。测试源文件 `test_remaining_upilot_todos.py` 和 `.meta` 不是 TODO 文档。
- 关联来源：`D:\MA\xclient\TODO_UPilot.mcd`、`F:\xclient2\TODO_UPilot.mcd`、`F:\xclient3\TODO_UPilot.mcd`。这些文件本轮只读，重复现场归并到下列产品项；KSB/关卡/弹道等项目业务事项不迁入通用产品开发清单，也不因此关闭。
- 历史验收参考：[统一定向验收记录](Documentation~/Unified-Backlog-Acceptance-20260920.md)。仅采用其中明确覆盖的结果，不用总体签署消除未覆盖的故障或跨版本矩阵。
- 优先级按剩余风险排列。部分完成、待定位、未开发、已实现待验证分别记录；忽略、排除不等于完成。后续验证默认仅做定向，不要求全量回归。

## 一、部分完成或待定位

### D01 / P1：公开 C# Capture 的持久停止证明

- 状态：公开 `VerifyStoppedAsync(sessionId)` 和能力版本 2 已实现；复用 Step 文件验真核心，区分 stopped 与 artifactsVerified，不自动 Stop。2026-09-24 定向 EditMode fixture 8/8 通过（含真实空文件及篡改），其余行为待验证。
- 剩余关闭条件：定向验证空文件/分段、历史缺文件、外部登记目录、重解析点、并发变更与取消，以及 Step 登记时句柄保护；不能以 inactive 推断产物成功。

### D02 / P1：反射现场歧义与执行边界定位

- 原 ID：`UPilot-20260922-NestedCallBinding`。状态：待定位，不能将全部现场直接判断为尚未开发。
- 代码：`Editor/Execution/Core/ExecutionCore.cs` 已优先非展开 params、较少默认参数；`Editor/Execution/Core/ReflectionCache.cs` 仍按同名方法枚举，绑定器未见声明类型优先的隐藏静态方法消歧。`Editor/Execution/Core/CSharpSubsetEngine.cs` 的调用参数先求值一次，再绑定。
- 剩余：针对 `JObject.Parse/JToken.Parse` 同签名隐藏静态候选补最窄复现；核对现场 SerializeObject、GetWindow、Type/GetField 和预算/finally 问题的实际加载身份及精确参数形状。重复参数求值只是历史假设，不能据此宣称已定位重复业务启动。
- 本批仅编写最小绑定参数形状 fixture，未运行、未定位根因，不更改反射绑定器。
- 关闭条件：原表达式和结构化调用的绑定/副作用边界可解释；无需重做已存在的 Type 值区分、闭合泛型、基础数组内联、向量序列化和 params 优先规则。

### D03 / P1：维护 inventory 的来源完整性和主 Editor 活体证据

- 原 ID：`UPilot-20260922-MaintenanceInventory`。状态：Bridge `queue.snapshot`、有界来源明细及 OS Editor 身份核验已实现；2026-09-24 两条 Python 定向用例通过（未跟踪工作、session 漂移），其余行为待验证，Server 新源码加载未核验。
- 剩余关闭条件：定向验证未识别队列、历史淘汰、截断、Reload/session 漂移、OS 拒绝及 PID 复用；不完整来源仍不得推断空闲。
- 既有精确 cleanup、独立授权、令牌、备份和 WriteBatch release 不重写；只读清单不得推进恢复或取消业务。

### D04 / P1：Bridge 大结果的超限诊断

- 原 ID：`UPilot-20260923-BridgeLargeResultFrame`。状态：Bridge 发送前 4 MiB 检查、有界命令错误/事件诊断和 Server 1009 来源记录已实现；2026-09-24 Unity UTF-8 计数边界用例和 Python socket shutdown 定向用例通过，其余行为待验证，Server 新源码加载未核验。
- 剩余关闭条件：确认边界字节数、首错保留、单次发送、事件丢失来源不完整及 1009 原命令未知时不重放。历史 1,316,191 字节定向帧已通过，不代表新逻辑已运行。

### D05 / P2：状态刷新生命周期中断归因

- 原 ID：`UPilot-20260922-StatusRefreshAbortDiagnostics`。状态：Reload、退出和显式停止的 generation/取消原因归因与 HTTP 异常诊断已实现；2026-09-24 定向测试发现取消已释放 CTS 时的 `ObjectDisposedException`，完成引用匹配修复及超时分支补强，两个原 WriteBatch 均已关联编译通过；第二批后精确 EditMode 用例 1/1 通过，真实 30 秒超时竞态及生命周期中断仍待验证。
- 剩余关闭条件：验证预期中断降级、普通缓存失效后的真实失败保留 Error、30 秒超时及过期结果不覆盖新状态。

### D06 / P2：通用 Operation 摘要的整体上限

- 来源：历史“Operation 状态响应缺少紧凑级别与输出上限”的 2026-09-23 新增子项。状态：summary 白名单、16 KiB UTF-8 整体限制、附件索引和计数已实现；2026-09-24 Python 定向摘要用例通过，真实 Server 新源码加载未核验。
- 现场：`maxTailChars=500` 时 26 个产物仍形成 13,420 字节响应，request=`req-8572b3a6-cca4-4bb4-bff7-b17147e44cf2`。
- 剩余关闭条件：定向核对多附件/深层对象/中文、低 maxTailChars、collect 后置字段和首错不变；完整清单仍通过现有 full/collect 获取。

### D07 / P2：验收 Task 摘要的白名单及截断证据

- 原 ID：`UPilot-20260921-AcceptanceCompactResponse`。状态：外层 Task 白名单、截断证据和诊断历史完整性字段已实现；2026-09-24 Python 定向摘要用例通过，真实 Server 新源码加载未核验。旧记录此前丢弃量保持 unknown。
- 剩余关闭条件：定向对照 summary/full 与产物，验证失败语义、编译及测试身份、历史丢弃字段；不重新开发异步验收入口。

### D08 / P2：Console 观察范围细分到清理和场景恢复

- 原 ID：`UPilot-20260918-01` 的阶段增量。状态：run/compile 级证据已实现并验收；细分阶段未见完整实现。
- 代码：`upilotserver~/src/upilot_mcp/console_evidence.py` 和 `domain/test_service.py` 保存起止范围及运行身份，未提供 assertion/cleanup/sceneRestore 各阶段的完整持久边界。
- 剩余：提供可用的阶段范围和缺口诊断，以区分断言后恢复场景产生的 Error；保持 `observed_during_run_not_causal`，不得推断因果或改写测试结果。旧客户端无 consoleEvidence 不算新实现失败。

### D09 / P2：Agent/Skill 蒸馏建议落地与在线证据边界

- 状态（2026-09-24 本批）：最小源码/文档修订、23 项定向 Python 测试、规范项目五目标同步及双安装校验完成；在线脚本说明仍为旧版，不将安装同步等同于 Server 部署验收。原项目级蒸馏历史和迁移入口中的有效观察、建议与边界已完整收敛到本节，临时文件随后删除。
- 权威源：`skills/upilot-unity-mcp/AGENTS.md.template`、`references/workflows.md`、`upilotserver~/src/upilot_mcp/mcp_tools/resource_tools.py`；复用既有 `references/safety.md` 与 `upilotserver~/scripts/p0_acceptance_call.py`，不新增客户端、摘要服务或调度器。
- 已收敛的五类原始观察：① Unity 2022.3 曾出现 Bridge 已更新但 Server 仍加载旧内存代码，证明 `/health` 和版本字符串不足以确认部署新鲜度；②替代 HTTP 端点曾需要手写 Streamable HTTP/SSE 调用和人工裁剪大响应，现复用既有有界证据客户端及精确项目核验；③原写批次身份曾被误路由到通用 Operation；④已知任务/Capture/Bridge 历史不能替代全量维护观察；⑤仅靠 `ConvertTo-Json -Depth` 无法约束大型验收报告。对应结论分别落实在下列 D09-A～E，不再依赖项目级临时文档。
- 共同分发证据：manifest 本批仅递增一次至 agentRulesVersion=45 / skillPackVersion=54，包版本仍为 0.3.35。源生成检查、source 校验及 `.agents`/`.claude` installed 校验通过；只同步 `D:\upilot\Tests~\UPilotTest`。最终实际重复 apply 返回 `changed=false`、五目标 `current`，46 个文件的集合/SHA256/mtimeNs 及 10 个备份目录均不变，受控块外字节保留。最终检查 requestId=`req-223b839b-c583-4cb2-9b89-def2ae898d17`；`loadedTemplateEvidence=unverified` 不升级为已验证。
- 定向测试：新增 11 项与直接回归 12 项合计 23/23 通过，无跳过；仅有 2 个既有 websockets 弃用告警。证据：[本批实施记录](Tests~/UPilotTest/Log/DistillImplementation/20260924/implementation-evidence.md)、`final-targeted-tests.xml`、`targeted-test-nodes.json`。合成测试与下列真实只读观察分别报告，不代表 Unity 编译/EditMode 或部署验收。

#### D09-A：原批次观察与身份路由表

- 来源/问题：2026-09-22 Eval/Emit 容量控制编译验证；编译 operationId 误用通用 Operation 查询导致 `OPERATION_NOT_FOUND`，最新快照也可能失去原批次关联。
- 源码/文档：Compile Fix 直接指定 `unity_write_batch_status(writeBatchId)`；区分 compileRequestId、通用 operationId、taskId、runGuid；删除不存在的 `unity_task_wait` 引用。关联缺失先查原批次，禁止重放编译或宣称原批次失败。
- 定向测试/安装：公开工具身份参数契约、原批次关联终态和 PlayMode 延迟批次直接回归通过；修订工作流已随 45/54 分发。
- 在线只读：原批次 `wb-ebe8a840-d0e4-49f0-8e05-5659f3eab8dd` 返回 terminal=true、verified/passed、errorsVerified=true、correlationVerified=true；compileOperationId=`0202dbb6ad5c4cc1931d5a1258aeb4a8`，compileRequestId=`req-bfa3ccc5-ee72-40ab-a3ef-8ab57b351a2e`，lastCompileVerifiedAt=1790173108420 晚于 writeBatchCreatedAt=1790173089197。观察 requestId=`req-c0910f2c-5870-4afb-b2bd-3cdf8cbe965f`。本次未触发编译，未使用最新快照替代原结果。

#### D09-B：全量维护观察范围不能由已知任务替代

- 来源/问题：2026-09-22 部署新鲜度核验；已知任务结束、Capture=0 或 Bridge 命令历史均不能证明队列完整或维护窗口空闲。
- 源码/文档：保留各来源、时效、完整性和截断边界；普通刷新不能将 incomplete 视为 idle；独立批准的 `unity_service_restart` 仍保留可中断在途工作的既有例外，未新增授权要求。
- 定向测试/安装：维护约束契约通过，工作流随 45/54 分发。
- 在线只读：`unity_queue_cleanup()` 返回 complete=false、isStale=false、`BRIDGE_QUEUE_NOT_FULLY_ENUMERATED`、maintenanceRisk=unknown；persisted/serverMemory/editor 有观察，bridgeCommands 为 unknown；7 条历史 Operation 中含 RecoveryRequired。requestId=`req-c659eaba-23a6-4edd-8512-d5b5591aebb6`。此结果按不完整解释，未执行清理、刷新或重启。
- 剩余边界：完整队列能力和 Server 加载身份仍未验收，产品缺口由 D03 跟踪；本子项仅完成工作流及不完整现场解释，不扩展产品能力。

#### D09-C：验收报告显式字段投影示例

- 来源/问题：2026-09-22 报告摘要；排除字段或 JSON Depth 不限制集合、字符串和 UTF-8 总体积。
- 源码/文档：区分 Task summary、完整报告与客户端 observation；按实际字段选择身份、计数、编译关联、清理和 artifact 元数据。集合最多 8 条、字符串最多 512 字符、总计不超过 16 KiB UTF-8；省略字段列表也限 8 条并保留总数；未知为 null，不序列化完整 error/raw state。
- 定向测试/安装：直接执行文档 PowerShell 示例的成功、业务失败、缺字段、大量错误、超长输出和 Task summary 六种合成用例通过；最终示例已随 45/54 分发。
- 真实文件：`Log/UPilotAcceptance/1790166630977_req-9dcb2d2a-7de0-430a-847a-729b006bc7e6/summary.json`，原文件 2,717,925 bytes，SHA256=`dac9b24fcedd3c2a071754f9fd4d392f7744111f9dac72380116e472fd44c032`；最终投影 1,066 UTF-8 bytes，计数/结果/清理字段与原报告逐项一致。原报告 acceptancePassed=false、测试计数均为 0，不能将此历史失败/无测试报告写成通过。原文件及哈希保留，未启动新验收。

#### D09-D：统一脚本 CRUD 的批次编译指引

- 来源：2026-09-24 蒸馏观察；工具旧说明与自动批次规则不一致。原观察依据、建议和实施结论已收敛于本项。
- 源码：仅修订三个脚本工具的注册 description，函数签名、返回与执行行为不变；整批只登记一次，删除放 deletedPaths、纯删除 paths=[]；自动批次只观察原 ID，不额外 sync/compile，PlayMode 等待权威 EditMode、不主动退出；手动分支仅适用于未启用自动批次。
- 定向测试/安装：三个注册说明契约通过；本项 Python 说明不靠 Skill 同步部署，关联工作流安装为 45/54。
- 在线效果：精确规范项目身份核验后，一次 SDK tools/list 实测三个说明均仍为旧版（Server PID=75316），见 `online-script-descriptions.json`。结论为“源码已修订、在线尚未生效”，非“已部署”。后续仅在另行授权的维护窗口部署并刷新客户端后重新观察，本次不重启。

#### D09-E：默认项目与明确授权替代目标的连接分支

- 来源：2026-09-24 蒸馏观察；业务授权已存在，但通用连接指引衔接不明确。原观察依据、建议和实施结论已收敛于本项。
- 源码/文档：仅修改权威 AGENTS 模板和工作流；默认规范项目不变，替代目标须已有业务规则明确授权且当前任务明确选择，先确定完整路径及独立 HTTP 端点，再核验实际返回路径；路径不符、未知或未选定即停止。不按 basename 放行，不改既有精确白名单，不把仓库 Unity2022 路径变为通用许可。
- 定向测试/安装：模板分支、替代端点精确路径及异路径拒绝、断连/未就绪不分发工具、业务块保留和安装上下文直接回归通过；规范项目五目标 45/54 已同步。
- 在线边界：只核验规范项目端点；未连接、同步或运行 Unity 2022.3 验收，未验证替代项目在线效果。删除临时蒸馏文档前已确认其有效内容完成收敛；Unity2022 已记录的 89 个基线文件哈希均未改变。

本批完成的是上述最小修订、定向测试、规范项目分发和明确列出的只读观察，不包含服务部署、发布、新 Unity 编译/测试、全量回归或 D03 产品扩展。后续活动状态与蒸馏结论只维护于本根清单，不再保留项目级蒸馏入口或历史副本。

### D10 / P1：共享进程下定向运行停滞的根因

- 原 ID：`UPilot-20260923-TargetedReliabilityAcceptance`。状态：待定位，不是已经证明的 MonoHook 产品缺陷。
- 已有记录：Python 两组 22+36=58 通过；独立 MonoHook 精确用例 1/1 通过。先前全量任务在邻近 MonoHook 阶段停滞后取消，runGuid=`486b18b7-b64f-4f1f-89ac-74827571dff9`，清理已确认。
- 剩余：基于 `Tests~/UPilotTest/Log/UPilotDiagnostics/unity-91960-1790166035965.dmp` 和原执行顺序定位共享状态/前序影响；必要时仅做最窄有界序列复现。单独 1/1 不能说明停滞根因已修复，也不授权补跑全量。
- 本批仅编写包含原 runGuid 与前序共享状态的最小 fixture，未运行，不据此判断停滞根因。

## 本批交付证据（2026-09-23）

- D03、D01、D06/D07、D04、D05、D09：代码或权威工作流说明已实现，相关定向 Python/C# 用例已编写但本轮**一项也未运行**；行为、故障注入及外部业务验证均待后续定向执行。D02/D10 仍待定位。
- 规范工程 C# 原批次：`wb-ebe8a840-d0e4-49f0-8e05-5659f3eab8dd`，compile operation=`0202dbb6ad5c4cc1931d5a1258aeb4a8`，request=`req-bfa3ccc5-ee72-40ab-a3ef-8ab57b351a2e`；关联终态 verified/passed，`correlationVerified=true`、`errorsVerified=true`、phase=completed；`lastCompileVerifiedAt=1790173108420` 晚于 `writeBatchCreatedAt=1790173089197`，编译错误 0、警告 3。修改前编译基线未记录，不补称已取得；随后仅编辑 Python、文档和 Skill 资源，没有追加 C# 保存批次。
- Console 查询还观察到 3 条 Error（MCP 自动启动批量进程查询格式、旧 TestRunner 意外 Reload 和旧窗口探针异常）；编译为 0 错误不表示 Console 为零错误。本批未调查或运行这些场景。
- Agent/Skill：权威分发资源 skillPackVersion=52，agentRulesVersion=44 未因本次仅修改 Skill 资源而递增；生成与源校验、五目标同步及重复 current、两份安装 Skill 校验通过，重复同步前后 46 个受管文件的内容与修改时间指纹一致（`DA003768B54BA515D5E9DB9FA6A8141091A55472D3E543FB1486660711C09006`），备份目录数保持 2。只证明磁盘分发一致性；Server 实际加载版本和行为均未核验，也未重启或发布。

## 2026-09-24 定向验收增量

- Python：对当前磁盘源码运行 `test_reliability_batch_inventory.py`、`test_reliability_batch_summaries.py`、`test_socket_shutdown.py`，10/10 通过。D02/D10 仅有形状 fixture，不能视为现场根因复现或修复。首次摘要断言误将 `responseDetailLevel` 的 `tail` 视作附件泄漏，修正为逐项检查附件后通过；未放宽 16 KiB 及脱敏要求。
- Unity EditMode：`CodingRiver.UPilot.Tests.Automation.UPilotConsoleCaptureApiTests`，runGuid=`255e86a9-69cb-49f3-96bf-2072f2edb43a`，8/8 通过、权威结果和 Runner 清理成立；覆盖空 Capture、篡改、Owner、异步读取、UTF-8 长度和生命周期异常分类，不证明重解析点、并发变化、真实 4 MiB 发送或网络故障。原运行期间 Console 出现状态刷新 CTS 已释放异常；本项通过不掩盖该运行时缺陷。
- D05 修复：`UPilotMcpServerManager.RunBoundedStatusRefreshAsync` 完成后仅在引用匹配时清空刷新 CTS 再释放，避免后续 `InvalidateStatusCache` 对已释放对象 `Cancel()`。原批次 writeBatchId=`wb-aa8d7224-7c4a-492a-90bf-8f5d96058476`、compileOperationId=`248010c1454a401e81e299c56edc0c1a`、compileRequestId=`req-d9394615-964c-40e6-bb23-f438aafc66f5`：verified/passed、`correlationVerified=true`、`errorsVerified=true`、`lastCompileVerifiedAt=1790206606125` 晚于创建 `1790206592730`，结构化错误/警告均为 0，该编译窗口 Console Error 为 0。修复后精确单条 runGuid=`86a40e55-0572-4396-81cc-f72e85d28fcf`，1/1 通过、`resultAuthoritative=true`、`cleanupSucceeded=true`、`unresolvedResources=[]`，该运行窗口 Console Error 为 0；尚未通过并发竞态或 Reload 故障注入验证。
- D05 超时分支补强：仅当超时分支仍持有原 CTS 引用时调用 Cancel，避免完成 continuation 已释放对象后的取消竞态。第二批 writeBatchId=`wb-e3eec267-ea24-4146-9682-010f28dfaf7e`、compileOperationId=`7c3854f4d23949d889dccd0134f42120`、compileRequestId=`req-edc65a4b-6261-4afb-88de-5630fbfca874`：verified/passed、`correlationVerified=true`、`errorsVerified=true`、`lastCompileVerifiedAt=1790206930320` 晚于创建 `1790206917733`，结构化错误/警告均为 0；编译窗口 Console Error 为 0，但跨 Domain Reload 覆盖为 partial。第二批后精确单条 runGuid=`2e90ade3-8709-4944-9f2b-9a1eafbf6199`，1/1 通过、`resultAuthoritative=true`、`cleanupSucceeded=true`、`unresolvedResources=[]`，该运行窗口 Console 覆盖 complete、Error 为 0。用例只覆盖生命周期异常分类，不证明真实 30 秒超时竞态或 Reload 中断行为。
- Agent/Skill 当前磁盘：`render_skill_pack.py --check`、源校验及 `.agents`/`.claude` 两份安装校验通过，`unity_agent_integrations_check` 五目标 `current`、skillPackVersion=53、agentRulesVersion=44；本轮没有再次同步或验证重复 apply 不改写文件，`loadedTemplateEvidence=unverified`。
- 部署边界：当前 MCP Server PID=75316 于本地 2026-09-23 19:57 启动，相关 Python 文件的修改时间为当日 22:00 至 22:27；时间顺序不能证明进程加载了最新源码，D03/D04/D06/D07 的实时 Server 行为仍未验收。没有为此重启服务、运行全量测试、清理历史任务或验证外部项目。下一步在授权维护窗口核对加载身份，再按原任务 ID 做狭窄真实流程观察；保留各 D 项上方列出的故障/边界关闭条件。

## 二、尚未开发或仅有候选方案

这些是独立新增能力或明确延期的增量，不把现有基础功能算作“从零未做”。

| 索引 | 推荐优先级 | 原条目/事项 | 当前基础与真实剩余 |
|---|---|---|---|
| N01 | P1，待评估 | 保留 PlayMode 现场的临时完整 C# 诊断模块 | Eval/Emit/Session 已存在；完整 C# 临时模块、生命周期及卸载边界未形成交付实现。先确认需求和安全边界，不扩大现有子集承诺。 |
| N02 | P2，待评估 | UPilot-20260920-MethodProfilingInstrumentation | Tracer/Profiler 已有；任意方法选择性插桩、PDB/调用点对应、ILPostProcessor/Cecil 候选和观测扰动原型待做。 |
| N03 | P2，未开发 | UPilot-20260922-CsvInsert | 编码保持的 query/patch 已有；新增记录的显式 insert、键冲突、dry-run/确认令牌、编码/换行/目标外字节保持待做。 |
| N04 | P3，未开发 | UP-030 脏场景下显式只读 EditMode 资源测试 | 脏场景拒绝/诊断已实现；只读声明与限定夹具放行待设计，默认阻断不能放宽。 |
| N05 | P3，未开发 | UP-021 Unity 原生 Tooltip 临时窗口截图 | 普通精确 EditorWindow 截图已有；Tooltip 独立宿主身份与像素来源验证待做。 |
| N06 | P3，延期 | Bridge 离线时按 projectPath 隔离的一次性 Worker | 当前启动/附着能力不等于离线 Worker；隔离目录、端口、身份、结果和退出清理待做。 |
| N07 | P4，延期 | 原生 Failed-to-load 空壳窗口识别 | 托管窗口生命周期已有；ContainerWindow/GUIView/OS 宿主关联及孤儿识别未实现。 |
| N08 | P4，延期 | 自动编译输入归属复用 | 原批次持久终态附着已有；登记前自动编译是否覆盖写入需 Unity 输入清单证明，不能仅凭时间相近、空闲或零错误关联。 |
| N09 | P4，外部待协调 | UP-016 Perception Skill 声明迁移 | 仓库内能力发现/代理已实现；独立所有权的外部 Skill 声明和 Registry 一致性迁移待做。 |
| N10 | P4，延期 | UP-020 历史文档/安装来源/稳定 ID 对齐 | 仓库自有生成检查已实现；外部安装来源及历史生态迁移尚未完成，不能为此重写 Registry。 |
| N11 | P1，待设计 | `csharp_eval` Emit 预热失败的严格后端路由 | 当前 `executionBackend=auto` 首次由 `interpret` 执行成功后再预热 Emit 缓存，`EVAL_CACHE_WARMUP_FAILED` 仅作为警告，不追溯改变本次结果；调用方无法声明严格后端要求，Emit 不可用时可能长期静默停留在解释执行，降级与性能变化缺少明确契约。后续应在业务副作用前完成兼容性判断和路由：Emit 不可用时优先切换到语义等价且支持当前 AST 的后端，无可接管后端则明确失败；返回实际后端、原始预热错误及降级原因，禁止在已成功执行并可能产生副作用后向其他后端重放。关闭条件：覆盖预热失败、可接管、无可用后端、异步及有副作用程序，证明不重复执行，并区分“当前调用成功但缓存预热失败”与“执行前没有可用后端”。 |

原始问题、来源和历史方案在 [根清单历史](UPilot-Backlog-History-20260923.mcd) 按原 ID/标题检索。前三项仍属于上一轮可靠性开发明确未纳入的能力，不自动转为本轮实施任务。

## 三、已实现但仍有验证或部署工作

本节不计入“尚未开发”。只保留具体缺失的证据；已通过的定向契约不要求重做。

| 索引 | 优先级 | 已有实现 | 剩余定向验证/部署工作 |
|---|---|---|---|
| V01 | P0 | 命令恢复白名单、原 commandId/截止时间、非白名单恢复错误 | 实际 Reload 后副作用零重放、已完成 Future、丢记录及迟到响应；核对 Task/Operation/Capture 原身份观察，不宣称 exactly-once。 |
| V02 | P1 | Runner 探测/取消/注销能力拆分、旧版 holder、按实际 API 注销 | 旧 UTF 无取消但自然结束可释放；反射未知保持 unknown；失败引用保留及真实双版本清理矩阵。 |
| V03 | P1 | WriteBatch 可信关联终态、事务落盘、原批次 safe wait、手动 deferred 精确 release | 并发附着、未知请求身份、历史证据损坏及释放竞争；不以最新编译覆盖原批次，不追加替代编译。 |
| V04 | P1 | 空 Capture 用 CreateNew 创建真实零字节 JSONL；业务首错与 cleanupError 分离 | 原任务/Operation 的零日志真实收尾、真实缺文件及重启后首错保持；公开原生终态证明开发缺口另见 D01。 |
| V05 | P1 | 验收公开入口立即返回持久 Task，preflightOnly 同步，私有执行路径 | 客户端断开、排队截止时间、Server 重启按原 runGuid 观察；无 runGuid 保持 RecoveryRequired，禁止重启业务补身份。 |
| V06 | P1 | 有界批量进程扫描、目标范围限制、停止前后身份核验、总维护预算 | 真实慢查询/PID 复用、多项目端口冲突、停止前失败和原 Bridge 恢复矩阵。已有双版本 13/13 与关联编译，不代表这些现场均通过。 |
| V07 | P2 | 验收恢复成功清旧错误、保留诊断和原子报告产物；源码变化保守归因 | 原 runGuid 成功恢复时各 gate、真实首错、产物写失败及重启查询的组合矩阵；未知文件不得当测试垃圾删除。 |
| V08 | P2 | QueueCleanup 精确处置、Operation 列表计数、Agent 来源/同步阶段与回滚诊断 | 新工具实际加载身份核验；同步失败/回滚/来源漂移现场。QueueCleanup 历史 Python112、Unity10/10 和五目标同步通过不等于旧 Server 已加载。Windows 访问拒绝根因仍未定位。 |
| V09 | P1/P2 | Step 公共支撑、Capture/Snapshot 所有权、附件登记和冻结报告恢复 | 实际 symlink/junction 路径、Snapshot 失败/超时/中途 Reload、实际可选无包编译。已有 140/140、50/50 和公开产物收集证据；外部业务矩阵不算产品未开发。 |
| V10 | P2 | Step 开发指南、Skill 全资源打包清单及安装校验 | 实际 EXE 构建产物是否携带完整资源仍待核验；此前未实际重建/发布，不能用清单测试替代交付产物证据。不得因本清单自动发布。 |
| V11 | P2 | 验收预检公开 importer-sensitive 清单及源码变化 | 双版本实际导入改变 .meta 时的 importState、预热与 source gate；相同哈希不独立证明导入完成。 |
| V12 | P2 | Test Runner 叶子增量摘要、cursor 与 gap/mismatch | 实际 Runner 通过/失败/延迟叶子、错误 run 回调、容量边界及取消中 Reload；已有 Python 契约不等于实机矩阵。 |
| V13 | P2 | UPilot-20260911-WindowFailureForensics 环形记录及身份复用 | 正常关闭/Reload 重建/OnEnable 失败、序号缺口及过期标记双版本矩阵；N07 原生空壳仍是独立未开发项。 |
| V14 | P2 | Capture 所有权、只读 attach/detach、片段和哈希 | 真实双客户端互不停止、轮转缺段及 Reload；仅存在接口或25项定向契约不能关闭完整矩阵。 |
| V15 | P2 | UP-015 Safe Mode 自有探针的 open/focus/精确截图 | 保留未覆盖的遮挡、模态/取消/自毁、Reload和最小化组合；不将自有白名单扩大为任意第三方窗口只读保证。 |
| V16 | P2 | UP-017 Operation 非法 JSON 的 Unicode offset/line/column/snippet；无法定位的 path 保持 null | 缺值/截断/非法转义/中文换行的实际 Bridge 结果和清理屏障。已有结构化位置诊断，不能宣称所有错误均可精确定位字段路径。 |
| V17 | P2 | Snapshot 正常/最小化/恢复路径 | 真实单轮已有证据；多轮窗口恢复及失败清理 fixture 尚未找到完成证据，属于测试建设/验证剩余，不是新增截图能力。 |
| V18 | P2 | UP-026 资源依赖类型区分、Prefab 组件对象引用解释 | 尚未找到覆盖所有既定引用/导入元数据场景的明确实机结论；保留有界现场矩阵，不能因旧标题待处理而重做序列化或引用查询。 |
| V19 | P1 | 显式发布证据/tag 标记校验、C→R 单父提交及版本绑定生成文件逐字节验证 | 2026-09-23 本地发布质量门禁 359 项通过；Windows 本地 `--verify-only` 已核对 0.3.35 EXE/manifest/校验文件及 commit，不代表远端验收。`release_evidence.py`、`check_release_quality.py` 的代码和本地回归已完成；真实受控 evidence run、tag 和下游构建端到端仍未验证，须独立发布授权。不恢复 UP-004 已忽略的旧强制证据要求，也不作为既往无证据发布失败的根因。 |

源码核对入口：`server.py` 的 `_resend_pending_commands`；`domain/compile_service.py` 的批次终态附着；`state_store.py` 的证据解释；`domain/test_service.py` 的 `_finish_acceptance_report`；`domain/task_service.py` 的持久任务恢复；`Editor/Core/UPilotTestRunnerAdapter.cs`、`UPilotConsoleCaptureService.cs`、`UPilotMcpServerManager.cs`；`Editor/Automation/Evidence`。

历史 C# 编译身份包括 `wb-dd0fdbf6-08b2-47fc-8b6f-f329e0980933`、`wb-0a03d6aa-a26d-4bea-b820-bb7aac04b7da`，以及进程扫描修复的规范工程 `wb-ae352373-bab2-4085-8d4a-a84a8614cd72` / Unity2022 `wb-26245156-2e85-46aa-bbae-18f127364121`。完整 operation/request/时间与测试身份保留在历史记录；这些不是本轮新执行的验证，也不证明当前部署已核验。

## 四、非活动范围

- 用户已忽略的 UP-002/004/006/007/009 剩余矩阵、Profiler 现场循环、空间充足的真实 full dump 和两个中断 dump 清理，不重新排期，不记为通过。第三方未接入 IMGUI 自动识别不因现场增补自动恢复开发。
- UP-018 的 HDRP Depth 和多 Display GameView 明确排除；当前支持边界不变。
- Eval named/ref/out 语法扩展仅保留未来边界，不是本轮承诺；现有验证、缓存和资源限制实现不重新列待开发。
- 外部项目 KSB/Case/关卡/业务 Skill、运行态场景等事项由项目清单维护。本轮没有分析全部业务实现或修改这些项目，不声称它们完成。

## 五、维护规则

所有当前产品待办统一维护在本文件，其他 TODO 入口不重复维护活动条目。新增项写明问题、原 ID/来源、最窄代码证据、真实剩余及关闭条件。父项已完成但有新问题时只保留增量；具体定向通过后从活动表移除并将证据归档。当前代码、历史验收、部署身份三者分开，不以“未验证”推导“未实现”，也不以“代码已实现”推导“已验收”。

## 六、2026-09-24 client2 Profiler 只读诊断增补

### D11 / P1：旧版 Test Runner 绑定失败造成 cleanup 高频全量持久化

- 状态：2026-09-24 已在权威源码 `D:\upilot` 实施性能与兼容性整改；规范测试项目 `D:\upilot\Tests~\UPilotTest` 通过关联 Unity 编译和定向 EditMode 回归。尚未修改或升级 `F:\xclient2` 中引用的 UPilot 包，也未对 Unity 2022.3 原现场做部署回归或非 Deep Profile A/B。
- 来源/身份：用户提供三张同一 EditMode 主线程帧截图；8013 HTTP MCP 实际连接 `F:\xclient2`、Unity 2022.3.62f2。该项目锁定 UPilot v0.3.35（Git `afa3a300cef9ce9b404aadffcadde95ad0050ed4`）与 UTF 1.1.33；Server/Bridge 完整加载源码指纹未验真，不把磁盘身份当作部署验收。
- 现场：runGuid `1cd7fd20-e211-4dc0-a8f9-a8243958307c` 自 2026-09-23 14:32:30.550 +08:00 进入 cleanup；86 个叶子已回报（58 passed / 28 failed），但 cleanupPending=true、runnerState=unknown、endedAt=0。实时 `unity_test_status` 报 `TEST_RUNNER_BINDING_UNAVAILABLE` / 缺少 `m_testJobDataHolder`；未启动、取消或清理该运行。
- 放大链：旧现场的 Runner 绑定失败会反复做反射候选与异常构造；100ms cleanup 探测即使状态未变也重复 PersistSnapshot；保存路径反复读取完整旧 JSON、JSON 深拷贝，并原子写主 JSON、`last-run.txt`、`active-run.txt`，每次都执行耐久刷新。未知 Runner 仍不得改判 inactive 来绕过清理屏障。
- 已实施：`UPilotTestRunnerAdapter` 按 API Type 缓存 adapter/反射绑定，兼容沿基类查找静态 singleton，并兼容 `TestRunnerApi.m_testJobDataHolder` 与 `TestJobDataHolder.instance` 的 property/field 形态；缺失绑定 diagnostic 只构建一次且继续报告 `unknown`。cleanup probe 改为 100ms 起步、最大 2000ms 的指数退避，状态变化恢复 100ms；runner/diagnostic/editor/cleanup/resources 均未变化时跳过重复持久化。持久化副本改为浅拷贝，旧快照冲突判断优先流式扫描根级 `snapshotSequence`/`endedAt`，解析失败才回退完整读取；内容未变化的 `last-run.txt`/`active-run.txt` 不再重复原子写和 `Flush(true)`。主 JSON 的原子替换、耐久刷新、stale writer 和终态防覆盖语义保留。
- 原始证据：截图 CleanupActiveRunFromUpdate 333.23ms / 3.1MB GC Alloc；PersistSnapshot 304.50ms；Save 301.31ms；AtomicWrite 三次合计 234.50ms，其中 Flush 三次 180.83ms；Read 43.02ms / 1.4MB；ProbeFrameworkRun 28.44ms / 19.2KB。父子累计值不可相加，单帧不代表平均耗时。只读观察的 JSON 为 258983 字节，snapshotSequence 从 282011 增长至 282102，尚不能据此断言固定 10Hz 或累计磁盘写入量。
- 验证：规范测试项目 Unity 6000.6.0a2 的关联写入批次 `wb-a5fc2c8e-7b6a-40e9-b2a3-1640e79da744` 编译通过，`errorsVerified=true`、0 error；`UPilotRunnerRecoveryTests` 22/22、`UPilotP1ReliabilityTests` 7/7 通过，均为 authoritative terminal，cleanup 成功、无 unresolved resource、无 persistence error。覆盖继承 singleton、缺失绑定缓存、unknown 语义、无变化 cleanup 跳写与退避、header scanner、unchanged pointer、并发/失败写入及恢复保护。
- 剩余关闭条件：在显式授权下，将修复版本部署到 Unity 2022.3/xclient2 原现场，验证 UTF 1.1.33 的真实 holder 绑定与 cleanup 收敛；关闭 Deep Profiling 后做相同场景/窗口/测试规模的短时 A/B，比较 CPU、GC、主线程 I/O 和持久化频率。未完成前不宣称现场 FPS 或绝对耗时收益。
- 项目补证/双向引用：`F:\xclient2\TODO_UPilot.mcd` / `UPilot-20260823-02` 的 2026-09-24 Profiler 性能补证。通用优先级/状态仍由本 D11 管理；原 28 个失败叶子的具体原因未逐项分析，不能归因于清理失败或改报通过。

### D12 / P2：窗口历史采样每轮构建完整窗口信息

- 状态：2026-09-24 已在权威源码 `D:\upilot` 实施轻量采样与对象复用；规范测试项目的关联 Unity 编译和窗口历史定向回归通过。尚未在 `F:\xclient2` 原现场做非 Deep Profile A/B。
- 原始证据：同帧 `UPilotWindowHistory.SampleOnUpdate` 343.70ms / 226.8KB。旧路径每 0.25s 新建字典、全量 `FindObjectsOfTypeAll<EditorWindow>()`，逐窗口构建完整 `EditorWindowInfo`，并读取历史并不需要的 input handle、focused window、`rootVisualElement` 等信息。普通 Sample 路径没有调用 Persist，不把其耗时误归因为窗口历史刷盘。
- 已实施：采样只读取历史语义所需的 wire instance ID、domain generation、type name、title、position 和 docked；不再调用完整 `UPilotWindowService.BuildWindowInfo`。复用当前窗口字典，domain generation 字符串按 Domain 缓存；窗口信息未变化时复用原 `WindowState`，避免每轮状态对象分配。`RecordSafeProbeEnabled` 同样使用轻量 capture，同时保留窗口消失、Reload、实例复用和历史序号语义。
- 验证：窗口历史精确回归 `EditorWindowHistoryRecordsNormalCloseWithoutInventingFailureAttribution` 与 `EditorWindowHistoryReusesUnchangedWindowState` 2/2 通过；结果为 authoritative terminal，cleanup 成功、无 unresolved resource。关联 Unity 编译同 D11，0 error。
- 剩余关闭条件：在相同窗口集合和采样模式下关闭 Deep Profiling，测量整改前后的 CPU/GC 与窗口数量，并验证真实窗口打开/关闭/Reload 恢复路径。本次截图主线程总计 9361.91ms，其中 `Profiler.IntegrateSessionData` 7775.69ms（83%），存在严重观察开销；不能把该单帧绝对值或 Deep Profile 状态当作正常编辑器基线，也不据此承诺 FPS 收益。
- 项目补证/双向引用：`F:\xclient2\TODO_UPilot.mcd` / `UPilot-20260924-WindowHistorySampling`；通用优先级/状态以本 D12 为准。`F:\xclient2` 引用包未修改。

### D13 / P2：下载性能用例缺少每个分片的失败、重试与实测并发证据

- 状态：待改进，不影响已落地的 Unity Editor Test Runner 网络基准用例；现有报告能记录端到端耗时、配置 4 分片、完成分片数、文件字节数与 SHA-256，但 OS 线程数及峰值 HTTP 并发只能标为 unknown。
- 观察与复现：2026-09-24 在 `Tests~/UPilotTest` 精确运行 `DownloadPinnedReleaseThroughUPilotAndWriteReport`（runGuid `a0b8e734-6d2d-4bbe-9a49-1cc6594ff9c1`），v0.3.37 Windows x64 EXE 的 4 分片下载耗时 172066 ms、0.1493 MiB/s、SHA-256 正确。运行期间曾观察到 `.part0` / `.part3` 文件长度回到 0，推测存在分片重试；现有状态/报告没有每分片尝试次数与错误，不能确认原因，也不能单凭该次速度将限速归因于 GitHub、客户端或本地网络。
- 建议：在生产下载器的只读状态/基准报告中以有限字段记录各切片的开始/完成时间、下载字节数、重试次数及已脱敏错误、实际并发峰值；保留固定文件、输出目录隔离与哈希校验，再做相同网络时段下的 aria2/UPilot 交错对照与单流/四分片定向比较。不要把配置的异步切片数写成真实 OS 线程数。
- 补充实测（2026-09-24，先 aria2 后 UPilot）：同一 v0.3.37 GitHub Release 文件、26942250 字节及 SHA-256，aria2（4 连接、split=4）耗时 20780 ms、1.2365 MiB/s；UPilot Unity 定向 EditMode 测试（runGuid `c5b16e16-c89a-425c-a3e7-84ba328e2c45`，4/4 分片）耗时 171456 ms、0.1499 MiB/s，慢约 8.25 倍。两边成功并校验一致；单次顺序对比排除了“GitHub 对所有客户端下载均只能到 200 KB/s”的笼统解释，但不能断定是重试、HTTP 实现或局部链路所致；UPilot 运行时曾观察到部分 `.part` 文件为 0 字节，但不能据此确认重试，需上条所述分片诊断验证。报告位于 `Tests~/UPilotTest/Logs/UPilotDownloadBenchmark/` 两个独立运行目录。

### D14 / P2：后台 Unity 编译请求长时间停留 queued 且前台恢复失败

- 状态：待改进；不属于本次 MCP Server 停止/重启修复。
- 观察：2026-09-24 canonical 项目 `Tests~/UPilotTest` 的登记批次 `wb-4e433bd7-fa0d-49f4-bcaf-ed9ae9080354` 于 `1790247507370` 请求编译，约 90 秒内一直处于 `compilePhase=queued`，Editor 心跳正常、`isCompiling=true` 但编译开始时间为 0。`unity_editor_focus_state` 指出 Unity 在后台；`unity_editor_focus` 返回 `FOCUS_NOT_ACQUIRED`。最终该批次以关联编译成功终结，不能将延迟误报成编译失败。
- 建议：在 `unity_write_batch_status` 的长时间 queued 诊断中区分 Editor 未聚焦/主线程持续运行/实际导入阻塞，提示可用且安全的等待/聚焦行动，并明确前台获取失败后的非破坏性处理。保留一次写批次/一次编译、不得自动重复发起编译。

### D15 / P1：MCP Server 重启后状态探测复用旧 HTTP 连接导致验证超时

- 状态：2026-09-26 已实施代码修复并完成 canonical Unity 项目定向验证；尚未发起新的 Server 重启，因此不能把原失败维护操作改写为重启验收成功。
- 观察与复现：维护操作 `restart-930c2a28806043a7a6ccafde6ff8cd6b` 已完成新 Server 进程和新 Bridge 会话替换，但 Unity 内长生命周期状态客户端持续在 `health_query` 阶段报告 `/stats` `HttpRequestException` 与 `/health` `TaskCanceledException`。同一 Unity 进程内由只读往返路径创建的新 `HttpClient` 能成功访问 `/health`，证明端点可用，故障集中在 Server 被替换后遗留的池化连接。此前另一次操作还暴露 `/health` 在 Bridge 会话尚未完整建立时应优先使用 `configured_project_path` 的身份字段。
- 影响工作流/工具：`UPilotMcpServerManager.GetFreshStatusAsync`、AI 服务维护重启验证的 `/health` 门、PID/精确项目路径和 Bridge 会话身份恢复。
- 已实施改进：loopback 状态客户端禁用代理；每个状态请求发送 `Connection: close`，避免跨 Server 进程复用旧连接；先请求权威 `/health`，再读取可选 `/stats`，统计端点失败不覆盖已成功的健康门；项目身份优先读取 `configured_project_path`，缺失时兼容回退 `project_path`。未放宽 PID、项目路径、部署身份、新会话或只读往返验证。
- 验证证据：写批次 `wb-03b0c6ea-85c5-4192-887a-ef8ff528ada5` 为 verified/passed，`errorsVerified=true`、`correlationVerified=true`、0 编译错误；修复后的 Unity 内真实 `GetFreshStatusAsync` 返回 `health=True`、PID `163176`、项目 `D:\upilot\Tests~\UPilotTest`、空失败阶段/错误；定向 EditMode runGuid `83ebd042-7551-4838-a93b-add3850ffe97` 3/3 通过且 cleanup 成功。
- 剩余关闭条件：在明确授权的后续维护窗口仅发起一次新的 `unity_service_restart`，观察 `/health`、PID/项目身份、部署身份和实际只读往返全部通过；不得自动重放上述失败 operation，也不得仅凭本次状态探测和单元测试声称重启验收已通过。